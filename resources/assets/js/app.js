
/**
 * First, we will load all of this project's Javascript utilities and other
 * dependencies. Then, we will be ready to develop a robust and powerful
 * application frontend using useful Laravel and JavaScript libraries.
 */

require('./bootstrap');

/*
$('button.delete').click(function (e) {
  e.preventDefault();
  var idbtn = $(this).attr('id');

  swal({
    title: '¿Estas Seguro de esto?',
    text: "Seleccione una de las opciones si desea Eliminar",
    type: 'error',
    showCancelButton: true,
    confirmButtonColor: '#f55549',
    cancelButtonColor: '#f55549',
    confirmButtonText: 'Si, Borra Esto!',
    cancelButtonText: 'No, Cancelar!',
    confirmButtonClass: 'btn btn-success',
    cancelButtonClass: 'btn btn-danger',
    buttonsStyling: false
  }, function (isConfirm) {
    if (isConfirm) {
      $('form[data-form="' + idbtn + '"]').submit();
    } else {}
  });
  e.preventDefault();
});

$('button.update').click(function (e) {
  e.preventDefault();
  var idbtn = $(this).attr('id');
  swal({
    title: '¿Estas Seguro de esto?',
    text: "Seleccione una de las opciones si desea Actualizar",
    type: 'warning',
    showCancelButton: true,
    confirmButtonColor: '#f55549',
    cancelButtonColor: '#f55549',
    confirmButtonText: 'Si, Edita Esto!',
    cancelButtonText: 'No, Cancelar!',
    confirmButtonClass: 'btn btn-info',
    cancelButtonClass: 'btn btn-danger',
    buttonsStyling: false
  }, function (isConfirm) {
    if (isConfirm) {
      $('form[data-form="' + idbtn + '"]').submit();
    } else {}
  });
  e.preventDefault();
});
*/
